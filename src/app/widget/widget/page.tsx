/**
 * Widget bundle endpoint
 * 
 * This page serves the React widget bundle
 * It's a standalone page that can be loaded as a script
 */

import Script from 'next/script';

export default function WidgetPage() {
  // This is a placeholder - the actual widget will be built as a separate bundle
  // For now, we'll serve it via a script tag
  return null;
}
